#include "main.h"

/**
 * _abs - compute absolute value of an integer
 *
 * n - is an integer
 *
 * Return: 0
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (0);
	}
	else if (n < 0)
	{ 
		return (-n);
	}
	else
	{
		return (n);
	}
}
